const fetchUrl = async () => { for (let i = 0; i < 5; i++) { try { const res = await fetch('http://localhost:3000/api/mossos-send', { method: 'POST', headers: { 'Content-Type': 'application/json', 'origin': 'http://localhost:3000', 'User-Agent': 'Mozilla/5.0' }, body: JSON.stringify({ reservation_code: 'HMDRKRWQSK' }) }); const data = await res.json(); console.log(data); return; } catch (e) { await new Promise(r => setTimeout(r, 2000)); } } console.log('Timeout'); }; fetchUrl();
